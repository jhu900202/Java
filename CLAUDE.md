# Java

이 리포지토리는 **자바 학습 기록**입니다. 개발자로 일하기 전(2018-12 ~ 2019-04) 학원 수료 과정에서 작성한 수업 실습(`src/study_YYMMDD[변형]`)과 과제(`src/report_YYMMDD`)를 당시 상태 그대로 보관합니다. 실무 코드가 아니며, 품질·보안 기준으로 평가하지 않습니다.

## 목적
- 과거에 무엇을 배웠는지 빨리 찾아보고, 기초를 복습하기 위한 기록입니다.
- 소유자는 현재 Java/Spring, JavaScript/React로 일하며, 같은 지식맵 규약을 다른 리포에도 적용할 계획입니다.
- 리포 정리(재구성) 여부는 소유자와 논의 중이며 아직 결정되지 않았습니다.

## 절대 규칙
- `src/`, `SMSlib/`는 소유자의 명시적 요청 없이 수정·이동·삭제·포맷·인코딩 변환하지 않습니다.
- 오타·버그·하드코딩·미완성 파일을 발견해도 고치지 않습니다. 해당 단원의 `## 메모` 또는 정리 논의 문서에 기록만 합니다.
- 소스에 있는 DB 계정, API 키·시크릿, 전화번호, 사설 IP의 **값**은 답변·문서 어디에도 옮겨 적지 않습니다. "어느 파일에 어떤 종류가 있다"까지만 적습니다.
- `src/study_190311/naver.html`, `**/*.jar`, `SMSlib/`는 읽지 않습니다.
- MS949 파일의 한글 주석은 `iconv -f CP949 -t UTF-8 <파일>`로 읽고, 파일은 재저장하지 않습니다.

## 지식맵 (knowledge-map/)
- 진입점은 `knowledge-map/ROOT.md` 하나입니다. 규약은 `knowledge-map/GUIDE.md`입니다.
- **ROOT를 읽는 경우**: 이 리포의 자바 개념 질문("상속 예제 어디 있었죠?"), 학습 이력·타임라인, 복습, 리포 정리 논의.
- **ROOT를 읽지 않는 경우**: git 명령·브랜치 작업, `.gitignore`/LICENSE 편집, 지식맵과 무관한 파일 조작, 이 리포와 무관한 일반 자바 질문.
- 순회: ROOT 로드맵 표의 `triggers`·`summary`로 단원 **1개**(경계가 겹치면 2개)를 골라 그 파일만 엽니다. `units/` 전체를 한꺼번에 읽지 않습니다.
- 패키지명이 보이는 질문("study_190318_mk3이 뭐였죠?")은 ROOT를 열지 않고 `grep -n "<패키지명>" knowledge-map/INDEX.md`로 해당 행만 읽습니다. 한 줄 요약으로 부족할 때만 그 행의 주 단원을 엽니다.
- GUIDE.md는 노드를 만들거나 frontmatter를 고치거나 규약이 헷갈릴 때만 읽습니다.
- 노드와 소스가 다르면 **소스가 진실**입니다. 그 사실을 답변에 밝히고 갱신을 제안하되 자동으로 고치지 않습니다.
- 세션 중 알아낸 사실은 해당 단원의 `## 메모` 섹션에 `- YYYY-MM-DD: 한 줄`로 추가할 수 있습니다. 새 노드 생성·본문 수정·표 변경은 소유자 승인 후에만 합니다.

## 고정 사실 (재조사하지 않음)
- 패키지 36개(study 31, report 5), `.java` 135개. 패키지 날짜 2018-12-17 ~ 2019-04-25(2018-12-26 → 2019-02-15 공백). 같은 날 변형은 `Mk2`/`_mk2`/`_room1` 접미.
- 원본 이력 56개 = 학원 시절 커밋 55개(2019-02-14 ~ 2019-04-29) + 2021-06-22 커밋 1개. 그 뒤 2026-09-25부터 지식맵 문서 커밋. 저자 `lpmarz3`/`Jaeorin`/`jaeorin`은 동일인(2019-02-20 ~ 03-14 두 계정 혼용, 03-14 'PC변경' 이후 Jaeorin만 사용).
- 인코딩 혼재: MS949 85 / ASCII 40 / UTF-8 9 / 기타 1. `study_181218/Var1.java`는 한글이 '占쏙옙' 형태로 깨진 채 커밋됨(복구 불가).
- 빌드 시스템·빌드 설정 없음. 컴파일을 시도하지 않습니다. 테스트 없음.
- 외부 jar: `SMSlib/`(javaSDK-2.2, json-simple-1.1.1, jsoup-1.11.3), `src/study_190304/ojdbc6.jar`(2.1MB).
- `.gitignore`는 `/bin/` 한 줄. LICENSE는 MIT.

## 언어·형식
- 소유자에게 보이는 설명은 한국어 존댓말. 파일명·frontmatter 키·slug·코드 식별자는 영어. 새 문서는 UTF-8(BOM 없음), LF.

## 정리 논의
- 리포 정리 이야기가 나오면 `knowledge-map/discussion/repo-reorg.md`의 "소유자에게 묻는 질문" 순서로 진행하고, 결정이 "결정 기록"에 적히기 전에는 아무것도 실행하지 않습니다.
